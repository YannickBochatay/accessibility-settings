{
  "name": "access-settings",
  "version": "0.0.16",
  "description": "web component for accessibility settings",
  "main": "dist/index.js",
  "files": ["dist/*"],
  "scripts": {
    "build-max": "esbuild src/index.js --bundle --format=esm --outfile=dist/index.js",
    "build-min": "esbuild src/index.js --bundle --minify --format=esm --outfile=dist/index.min.js",
    "build-api": "esbuild src/settings/index.js --bundle --format=esm --outfile=dist/api.js",
    "build-api-min": "esbuild src/settings/index.js --bundle --minify --format=esm --outfile=dist/api.min.js",
    "build-example": "esbuild doc/components/index.js --bundle --minify --format=esm --outfile=doc/components/index.min.js",
    "build": "npm run build-max && npm run build-min && npm run build-api && npm run build-api-min",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YannickBochatay/access-settings.git"
  },
  "keywords": [
    "accessiblity",
    "webcomponent"
  ],
  "author": "Yannick Bochatay",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/YannickBochatay/access-settings/issues"
  },
  "homepage": "https://github.com/YannickBochatay/access-settings#readme",
  "devDependencies": {
    "esbuild": "0.27.2"
  }
}
